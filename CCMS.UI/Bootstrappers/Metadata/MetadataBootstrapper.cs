using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;
using System.Windows;
using System.Windows.Data;
using System.Windows.Controls;

namespace CCMS.UI.Bootstrappers.Metadata
{
    public static class MetadataBootstrapper
    {
        public static void Initialize()
        {
            //var metadata = new FrameworkPropertyMetadata(
            //    string.Empty, FrameworkPropertyMetadataOptions.BindsTwoWayByDefault, 
            //    null, null, true, UpdateSourceTrigger.PropertyChanged
            //);

            //TextBox.TextProperty.OverrideMetadata(typeof(TextBox), metadata);
        }
    }
}
