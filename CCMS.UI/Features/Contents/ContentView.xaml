<UserControl x:Class="CCMS.UI.Features.Contents.ContentView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:uc="clr-namespace:CCMS.UI.Features.Contents"
             mc:Ignorable="d" d:DesignHeight="300" d:DesignWidth="300">
    
    <DockPanel HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Height="Auto">
        <!-- credit card information section -->
        <!--<uc:CardInfoView DataContext="{Binding CardInfo}" DockPanel.Dock="Top"/>-->
        <uc:CardInfoView DataContext="{Binding CardInfo}" DockPanel.Dock="Top"/>
        <uc:CardTransactionView DataContext="{Binding CardTransactions}"/>
    </DockPanel>

</UserControl>
